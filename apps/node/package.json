{
  "name": "@workspace/p2p-spike-node",
  "version": "0.0.0",
  "private": true,
  "description": "Standalone smoke harness: two NodeRuntime instances find each other via real Hyperswarm and replicate a log.",
  "type": "module",
  "scripts": {
    "smoke": "node --experimental-strip-types --no-warnings src/smoke.ts",
    "demo": "node --experimental-strip-types --no-warnings src/end-to-end.ts"
  },
  "dependencies": {
    "@workspace/p2p-runtime": "*",
    "@workspace/portable-bootstrap": "*",
    "@workspace/ucan-boundary": "*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
